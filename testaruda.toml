# testaruda configuration for Testimonial.jl
# See: https://github.com/charly-vibes/testaruda

[adapters]
# Julia adapter — runs Testimonial.jl's protocol adapter
# Uses the shell wrapper script; needs the full path to the script.
# If Testimonial.jl is installed via Pkg.add, the path is:
#   $(julia -e 'print(dirname(dirname(pathof(Testimonial))))')/bin/testaruda_adapter.jl
# For local dev:
".jl" = "julia --project=$(pwd) bin/testaruda_adapter.jl"

# Default adapter (same as Julia adapter for now)
default = "julia --project=$(pwd) bin/testaruda_adapter.jl"

[discover]
# Julia test discovery: ReTestItems.jl @testitem blocks
# Pattern: test/ directories with .jl files containing @testitem
paths = ["test/"]

# Exclude common build artifacts and vendored deps
exclude = ["target", ".git", "node_modules", ".venv", "venv",
          "__pycache__", ".mypy_cache", ".pytest_cache",
          "build", "dist", ".tox", "deps", "docs/build"]

[store]
path = ".testimonial/testaruda"

[oracle]
# Soufflé Datalog oracle for cross-validation (optional)
# Install: https://souffle-lang.github.io
enabled = false
